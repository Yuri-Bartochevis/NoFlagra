# Secrets — never commit these (applies at any depth: edge/.env, cloud/.env, ...)
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Editors / OS
.DS_Store
.vscode/
.idea/

# Generated by cloud/freeze.py — rebuild it, don't commit it, so the static
# copy can't drift from the templates it came from.
dist/
