flask==3.0.3
gunicorn==22.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.7
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# Phase 3: signing R2 upload/playback URLs. R2 speaks the S3 API, so this is
# the same client either way — only R2_ENDPOINT changes.
boto3==1.35.36
